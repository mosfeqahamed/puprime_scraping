# MongoDB Cloud (Atlas) Configuration
# Get your connection string from MongoDB Atlas dashboard
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-name>.mongodb.net/?retryWrites=true&w=majority

# Example MongoDB Atlas connection string:
# MONGODB_URI=mongodb+srv://myuser:mypassword@cluster0.abc123.mongodb.net/?retryWrites=true&w=majority

# Optional: Custom database name (default: puprime_data)
# DATABASE_NAME=puprime_data

# Optional: Sync interval in hours (default: 6)
# SYNC_INTERVAL_HOURS=6
